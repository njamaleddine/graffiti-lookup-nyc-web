name: Test Python

on:
  push:
    branches: [main]
    paths:
      - 'geocode/**'
      - 'setup.cfg'
      - '.github/workflows/test-python.yml'
  pull_request:
    branches: [main]
    paths:
      - 'geocode/**'
      - 'setup.cfg'
      - '.github/workflows/test-python.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: geocode/requirements-dev.txt

      - name: Install dependencies
        run: pip install -r geocode/requirements-dev.txt

      - name: Run tests with coverage
        run: pytest geocode/tests -v --cov=geocode --cov-report=term-missing

      - name: Check coverage threshold
        run: pytest geocode/tests --cov=geocode --cov-fail-under=80